---
plugins:
    auctions.lease:
      aliases: []
      use_default: true
      migration: false
config:
  main:
    auth.file: src/openprocurement/lease/tests/auth.ini
  database:
    couchdb.db_name: flash_tests
    couchdb.url: http://op:op@localhost:5984/
